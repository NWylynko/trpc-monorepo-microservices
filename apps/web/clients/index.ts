import { auth } from "./auth";
import { groups } from "./groups";
import { messages } from "./messages";
import { users } from "./users";

export const client = {
  auth,
  groups,
  messages,
  users
}